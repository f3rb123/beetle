.claude/
.codex
node_modules/
venv/
.venv/
__pycache__/
*.pyc
.env
.env.*
dist/
build/
coverage/
*.log
.vscode/
.idea/

# Phase 9.1.5 secret-safety validation scratch (jadx binary + decompiled APKs)
.tools/

# Validation / review scratch artifacts (never commit — may contain tokens)
.val_token
.val_ids
.val*
.valout/
.uireview/
frontend/preview-live/
frontend/public/render-ws.html
*.apk
*.ipa
uploads/
reports/
tmp/
*.zip 
 
*.pdf
internal/MISSING_FEATURES.md

# Beetle production-run planning docs (local working files, static)
CLAUDE.md
BEETLE_PROMPTS.md
# BEETLE_PROGRESS.md is deliberately TRACKED — it is the crash-recovery / cross-session
# resume record, which only works if it survives in version history.

# Test binaries (never commit large app files)
testapps/
*.apk
*.ipa

# RUN 31+ — scan artifacts used for before/after report diffing (large, regenerable)
baseline/
after_run*/
